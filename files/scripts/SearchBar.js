

class SearchBar {
    static search(type) {
        if (arguments.length === 0 || type === 'video') {
            // TODO YTAPI
        } else if (type === 'playlist') {
            // TODO go through user playlists
        }
    }
}